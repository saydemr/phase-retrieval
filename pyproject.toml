[project]
name = "phase-retrieval"
version = "0.1.0"
description = "X-ray Wave Propagation and Phase Retrieval"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "matplotlib>=3.10.8",
    "numpy>=2.4.2",
    "scikit-image>=0.26.0",
    "scipy>=1.17.1",
]
